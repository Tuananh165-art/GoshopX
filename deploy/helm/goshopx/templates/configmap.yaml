apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "goshopx.name" . }}-config
  labels:
    app.kubernetes.io/name: {{ include "goshopx.name" . }}
data:
{{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
{{- end }}
